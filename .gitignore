# deps / build
node_modules/
site/dist/
site/.astro/

# tooling artifacts
.playwright-mcp/

# personal media and raw references - never push these
refs/

# desktop font sources (EULA forbids redistribution; web subsets in
# site/public/fonts are the embedded webfonts and stay tracked)
fonts/

# ascii art sources (large originals; committed output lives in site/public/art)
art/source/

# raw full-page captures (outside public/ so they never ship; webp is committed)
captures/

# scratch pages kept out of routing
*.astro.bak


# raw project exports dropped in for processing
якитория/
Десктоп/
Мобайл/
work/
*.svg
!site/public/**/*.svg
